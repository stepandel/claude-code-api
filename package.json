{
  "name": "cantelop-claude-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cantelop Edge API and Session actors hosting unmodified Claude Code",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "cantelop dev",
    "build": "cantelop build",
    "check": "tsc -p tsconfig.api.json && tsc -p tsconfig.session.json",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@cantelop/sdk": "0.9.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3"
  }
}
